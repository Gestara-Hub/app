# Projetos da v1 (não versionar — recomeço a partir das specs em docs/)
old/

# dependencies (monorepo: pnpm faz hoist para node_modules na raiz)
node_modules/
.pnpm-store/

# build / cache
.next/
out/
build/
*.tsbuildinfo
next-env.d.ts

# macOS
.DS_Store

# Claude Code (config pessoal/local)
.claude/settings.local.json
